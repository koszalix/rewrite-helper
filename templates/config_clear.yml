api:
  host:
  port:
  proto:
  username:
  passwd:

ping_jobs:
  - job:
      domain:
      interval:
      count:
      timeout:
      answers:
        primary:
        failover:
          -
          -
http_jobs:
  - job:
      domain:
      interval:
      status:
      proto:
      port:
      answers:
        primary:
        failover:
          -
          -
