# This config file is for test use only.
http_jobs:
  - job:
      domain: test.com
      interval: 30
      status: 201
      proto: https
      port: 8080
      answers:
        primary: 1.1.1.1
        failover:
